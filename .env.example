# Copy this file to .env and adjust the values. The CLI and the dashboard
# load it on startup, and real environment variables take precedence over it.
# No API key required, everything runs locally.

# Where the SQLite database lives (default: output/shopflow.db)
# DATABASE_PATH=output/shopflow.db
